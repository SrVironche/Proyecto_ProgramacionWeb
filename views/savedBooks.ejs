<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mis Libros Guardados</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #121212;
            color: #e0e0e0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
        }
        h1 {
            color: #fff;
            margin-bottom: 20px;
        }
        ul {
            list-style-type: none;
            padding: 0;
            width: 90%;
            max-width: 800px;
        }
        li {
            background-color: #1e1e1e;
            margin-bottom: 15px;
            padding: 15px;
            border-radius: 8px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.3);
            color: #e0e0e0;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        img {
            max-width: 60px;
            border-radius: 5px;
            margin-left: 15px;
        }
        a {
            color: #bbb;
            text-decoration: none;
            margin-top: 20px;
            padding: 10px;
            background-color: #333;
            border-radius: 5px;
            transition: background-color 0.3s;
        }
        a:hover {
            background-color: #666;
            color: #fff;
        }
        p {
            color: #aaa;
        }
    </style>
</head>
<body>
    <h1>Mis Libros Guardados</h1>

    <% if (books.length > 0) { %>
        <ul>
            <% books.forEach(book => { %>
                <li>
                    <div>
                        <strong><%= book.titulo %></strong><br>
                        Autor(es): <%= book.autor ? book.autor : "Desconocido" %>
                    </div>
                    <img src="<%= book.imagen_url %>" alt="Portada">
                </li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>No tienes libros guardados.</p>
    <% } %>

    <a href="/auth/dashboard">Volver
